import type { Message, UserMessage, AssistantMessage, ToolResultMessage, TextContent } from '@mariozechner/pi-ai/dist/types.js';

export type AgentMessage = Message;

export type { Message, UserMessage, AssistantMessage, ToolResultMessage, TextContent };
