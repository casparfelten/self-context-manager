# Write-down — High-level Things to Work On

Date (UTC): 2026-03-08T00:16:36
Purpose: planning checkpoint only (no implementation changes in this note)

## Canonical doc map (current)

1. `docs/intent-ssot-v1.md` (intent SSOT)
2. `docs/implementation-db-ssot-v1.md` (DB implementation SSOT)
3. `docs/implementation-agentic-ssot-v1.md` (agentic implementation SSOT)

## Current implementation status (as observed)

- Specs are refactored and split into intent + DB + agentic SSOTs.
- Runtime code is still XTDB-era (`src/xtdb-client.ts`, `src/phase3-extension.ts`).
- New SQLite implementation per SSOT is not yet implemented.

Evidence snapshots from this session:
- `npm test --silent`: 239 passed, 21 failed; failures are XTDB connection errors (`ECONNREFUSED 172.17.0.1:3000`) in phase1/phase3/phase4/e2e tests.
- `npm run build --silent`: TypeScript errors in `tests/ssot-conformance/session-lifecycle.test.ts`.
- `rg` in `src/` found no SQLite/StoragePort implementation surface aligned to new SSOT.

## High-level work items (priority order)

### 1) Implement DB layer to SSOT (SQLite)
- Build the storage adapter matching `implementation-db-ssot-v1.md`.
- Include schema, transactional `putVersion`, idempotency, optimistic conflict, hash writes, and `doc_references` extraction.

### 2) Define and implement concrete `StoragePort` boundary in code
- Materialize interface/types in `src/` as the implementation contract.
- Ensure all reads/writes route through this boundary.

### 3) Migrate agentic context-loading layer off XTDB client
- Refactor extension/context runtime to consume only `StoragePort` queries.
- Enforce no direct SQL/no XTDB HTTP dependency in loader layer.

### 4) Session semantics implementation pass
- Implement session HEAD resolution and set handling (`active_set`, `inactive_set`, `pinned_set`) via immutable version writes.
- Keep `session_id` typed envelope semantics consistent with SSOT.

### 5) Test surface migration
- Replace XTDB-dependent phase tests with SQLite-backed integration tests.
- Keep/refine SSOT conformance tests against the new boundary.
- Add focused tests for idempotency replay vs mismatch, ref extraction determinism, and unresolved refs.

### 6) Remove or archive XTDB runtime remnants from active implementation path
- Move XTDB-only runtime code/tests/scripts behind archive or legacy marker once SQLite path is live.

### 7) Live-drive validation after implementation
- Run real end-user flows (not just unit tests) through the new agentic layer + SQLite backend.

## Open clarifications to lock before coding wave

1. Closed enum vs open string policy for `ref_kind` in v1 code.
2. Exact object-type payload field matrices (to avoid implementation drift).
3. Whether watcher behavior is in v1 scope or explicitly out of active implementation.

## Next-step pointer

Start with item 1 (DB layer) and item 2 (StoragePort) as a single implementation wave; do not start agentic refactor before storage boundary is executable.
