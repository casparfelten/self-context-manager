# Write-down — DB SSOT test rewrite wave

Date (UTC): 2026-03-08T01:10:20
Scope: tests only (DB SSOT conformance)

## Method (fixed)

1. Use `docs/implementation-db-ssot-v1.md` as the only rule source.
2. Write explicit tests for each rule block (profile, invariants, schema, indexes, putVersion contract, refs, session semantics, hashes, boundary, out-of-scope).
3. Avoid placeholder tests (`expect(true)` etc.); every test must assert a concrete DB/API behavior.
4. Add query-plan checks (`EXPLAIN QUERY PLAN`) for index-use evidence, not just index existence.
5. Keep runtime choice fixed to the current SQLite implementation under test (`SqliteStorage` + real SQLite file).
6. Execute full DB test suite and record the result.

## Executed

### Rewritten/new DB test suite

- `tests/storage/ssot-db-core-invariants.test.ts`
- `tests/storage/ssot-db-schema-indexes.test.ts`
- `tests/storage/ssot-db-put-version-contract.test.ts`
- `tests/storage/ssot-db-refs-session-hashes.test.ts`
- `tests/storage/ssot-db-boundary-and-profile.test.ts`
- `tests/storage/ssot-db-query-plan.test.ts`
- `tests/storage/test-helpers.ts`
- `tests/storage/SSOT_DB_TEST_MAP.md` (rule-to-test map)

Removed old mixed test file:
- `tests/storage/sqlite-storage.test.ts`

### Evidence run

Command:
- `npx vitest run tests/storage`

Observed:
- Test files: `6 passed (6)`
- Tests: `51 passed (51)`

## Notes

- This wave intentionally focused on DB SSOT conformance tests only.
- Query-plan tests now check usage of `idx_versions_session_id`, `idx_refs_target_hash`/`idx_refs_target_version`, and partial `idx_refs_resolved`.
