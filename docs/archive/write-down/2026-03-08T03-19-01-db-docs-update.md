# Write-down — DB docs update after conformance re-check

Date (UTC): 2026-03-08T03:19:01
Scope: documentation update only (no code change)

> Historical snapshot note: this file captures repo state at 2026-03-08.
> It is non-normative and superseded by later session-validation work (`invalid_session_id`) and newer conformance evidence.

## What was updated

1. Updated `tests/storage/SSOT_DB_TEST_MAP.md`
   - Added explicit mapping for new `tests/storage/ssot-db-refs-hash-spec.test.ts`.
   - Updated §6.4 mapping to reflect exact SSOT assertions now covered:
     - `from_path` exact sensitivity
     - `ref_metadata` exclusion from `refs_hash` tuple
   - Added a "Known coverage gaps (current)" section for remaining test insufficiencies.

## Evidence used for this doc update

- `npx vitest run tests/storage`
  - Observed: `7 passed (7)`, `53 passed (53)`.
- `npm test --silent`
  - Observed: `11 passed (11)`, `74 passed (74)`.
- Direct runtime probe (Node script against `dist/src/storage/sqlite-storage.js`)
  - Observed: `session` write accepted with `sessionId: null` (`ok: true`).
  - This is recorded as a current coverage/contract-clarity gap in the test map.

## Current-state summary

- refs-hash SSOT mismatch noted earlier appears fixed in code + targeted tests.
- Remaining gaps are now documented explicitly in test-map docs instead of being implicit.
