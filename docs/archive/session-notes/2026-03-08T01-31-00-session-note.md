# Session note — runtime migration to StoragePort

Date (UTC): 2026-03-08T01:31:00
Scope: migrate active runtime (`SelfContextManager`) to DB SSOT `StoragePort` boundary and remove legacy backend dependency.

## Fixed plan

1. Replace direct legacy client dependency in `src/phase3-extension.ts` with `StoragePort` usage.
2. Default runtime storage implementation to `SqliteStorage` (file-backed, deterministic path).
3. Keep public runtime API stable where practical.
4. Persist session state using SSOT session payload shape (`chat_ref`, `active_set`, `inactive_set`, `pinned_set`).
5. Keep metadata pool continuity in `metadata_json`.

## Executed

- Reworked `src/phase3-extension.ts` to use `StoragePort` exclusively.
- Default storage path:
  - `<workspaceRoot>/.pi/self-context-manager.sqlite` when `workspaceRoot` is set
  - `<tmpdir>/self-context-manager/.pi/self-context-manager.sqlite` otherwise
- Added runtime history helper (`getObjectHistory`) and entity projection helper (`getEntity`).
- Updated runtime tests to verify history and entity behavior through the runtime boundary.

## Conformance fix recorded

- `refs_hash` now follows DB SSOT §6.4 tuple exactly in storage implementation:
  `(from_path, ref_kind, target_object_id, target_version_id, target_object_hash, mode)`
- No path normalization, no metadata contribution.

## Evidence snapshot

- Targeted runtime + storage suites passed in this session.
- Remaining failing tests in full suite were isolated to removed legacy integration tests and are not part of active SQLite path.
